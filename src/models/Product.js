class Product {
  constructor(id, name, category, brand, price, stock, state) {
    this.id = id;
    this.name = name;
    this.category = category;
    this.brand = brand;
    this.price = price;
    this.stock = stock;
    this.state = state;
  }
}

export default Product;
